# Uncomment the next line to define a global platform for your project
platform :ios, '10.3'

target 'SwiftShare' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftShare
   pod 'Alamofire'
   pod 'RxSwift'
   pod 'SwiftyBeaver'
   pod 'SwiftLint'

  target 'SwiftShareTests' do
    # Pods for testing
  end

end
